- data items:
  - parse the movie titles to make release year a feature.
     - that also needs fourier encoding in the model
  - consider making the hour of day crossed with weekday feature more course than 24 hour bins
  - since time is important in the TwoTower Query model,
    for using data spanning years larger than the categorical bins
    of users age in users.dat, one would want to adjust the
    ages in the ratings-user-movie join during ingestion.
  - add ZCTA and geohashing to transform the user zipcodes
    to proximity aware values
    - could also consider a feature that includes size of the
      ZCTA demographic into large medium small
  - supplement the movies data (needed for the movie tail distribution!)
     - MM-ML-1M (Multimodal MovieLens 1M)
       - summary says rich data to supplment the users and movies of ML-1M
     - KGAT (by Xiang Wang) or KGCN (by Hongwei Wang) for director graph data.
  - consider changing partition size based on expectations regarding 
    data shifts from train to test and retraining on new data, etc

- change the bi-encoder to use pretrained LLM for
  the User and Movie properties that are not
  user_id and movie_id.  use a prompt formatting
  and version control for the inputs.
  note that missing information from a query can 
  use "unknown".  the rest of the mode remains
  the same.  see google doc folder for models to
  try.
  - this is done in reranker, so would need to consider
    pros cons of new tiers or single tier recommendations
    and compare the metrics.

- consider implementing a Jax MLOps pipeline 
 (ranker project uses JAX AI stack so can start there, w/ caveat 
 that a TFX pipeline w/ MLMD needs less customization)
  with Jax, Grain (w/ ArrayRecord format), 
  Flax/NNX, Optax, Orbax, XLA
  and possibly Pallas and Qwix.
  Orbax can write to Vertex AL Metadata (replacing MLMD).
  Vertex AI pipelines using Kubeflow v2.
  - serving Jax on Vertex AI uses vLLM or JetStream
  - OCI artifacts to house the Grain and Flax logic)
  - Vertex Model Registry and Model Evaluation are used
    in the MLOps.

- consider adding to pipeline:
  - transform_input_cache

- reconsider better way to pass best parameters used
  in final model to the Retrieval model (see other project).
  e.g. the signature for the serving_query model needs
  inputs that have all of the joined raw ratings input columns, 
  but the only columns
  it will use is user_id and age when feature_acronym="a".
  similarly, the serving_candidate models needs movie_id and genres.

  restructure:
- consider adding other model families to the
  pipeline, that is, a wrapper to use Tuner on
  each model family and then compare among best of each.
  - consider using SNAS to build a good model
    - transformer layers, dot product layers,
      feature interaction layers, embedding layers, ...
  - best practices for optimizing multi-tower NNs
  - best practices for optimizing mixture of experts (MOE) NNs

- consider adding 
  NannyML OSS: Performance estimation 
  - CBPE (for classification) and DLE (regression)

- see TODO throughout code
 
- future:
  consider modifying the custom ingest components to add 3 properties 
  to make the data and configuration self-consistent:
    add properties max_user_id,max_movie_id, and n_genres
    to the output_examples.
    add dataset count from beam.combiners.Count.Globally().
    add versioning of datasets.
  the standard_artifact.Examples would need to be extended to
  include those properties:
    class AugmentedExamples(standard_artifacts.Examples):
      max_user_id = Property(type=PropertyType.INT)
      max_movie_id = Property(type=PropertyType.INT)
      n_genres = Property(type=PropertyType.INT)
  Then in the Do method of the Executor:
    use PTransforms to calculate those numbers from the 
    input users.dat and movies.dat files.
    And set them in the output:
    iutput_examples_artifact = artifact_utils.get_single_artifact(
        output_dict['output_examples'])
    output_examples_artifact.max_user_id = max_user_id
     ...
   -> problem is that currently, cannot pass that data as Channels
      to components that could use them like Tuner and Trainer
- CI/CD
  - a linter usable w/ terraform:
    https://www.markcallen.com/tflint-static-analysis-for-terraform/
  - consider tindying up the use of pre-processsing scripts including eda,
    running the pipeline, running script to analyze trained model,
    and copying approved trained model outputs to other locations.

- consider a more advanced model that uses a lightweight transformer and
  DCN netowrk layer to make richer, time aware embeddings:

(Sequence-Aware & Cross-Feature Two-Tower)
│
├── Hyperparameters & State
│   ├── temperature (τ)                          # Softmax scaling factor
│   ├── log_q_correction_factor (β)              # Log-Q bias correction multiplier
│   ├── table_A & table_B                        # State tracking physical time gaps between appearances
│   └── max_seq_len                              # Length of user watch history sequence (e.g., last 50 movies)
│
├── QueryModel (User / Context Tower with Sequence Transformer & DCN-v2)
│   ├── UserModel (Feature Extractor)
│   │   ├── Categorical & Dense Embeddings       # Static features: age, hr, weekday, hr_wk, month, yr
│   │   │
│   │   ├── DCN-v2 Stack (Cross Network)         # Explicit Bounded Feature Interactions
│   │   │   ├── Cross Layer 1 (x₀ · xᵀ · W₁ + b₁ + x₁)
│   │   │   └── Cross Layer 2 (x₁ · xᵀ · W₂ + b₂ + x₂)
│   │   │
│   │   └── Sequence Transformer (e.g., SASRec / BERT4Rec)
│   │       ├── Item Embedding Table             # Shares weights with Candidate Model item embeddings
│   │       ├── Positional Embeddings            # Encodes relative recency of past watched movies
│   │       ├── Multi-Head Self-Attention        # Learns dynamic temporal intent across watch history
│   │       └── LayerNorm & Feed-Forward (FFN)   # Outputs fixed-size sequence representation
│   │
│   ├── Concatenation & Fusion Layer             # Concat: [DCN-v2 Output | Transformer Seq Vector | Static Embeddings]
│   │
│   ├── MLP Stack (Deep Layer)
│   │   ├── Dense Layer 1 (e.g., 128 units + GELU)
│   │   └── Dense Layer 2 (e.g., 64 units)
│   │
│   └── call(inputs)                             # Fusion Vector ➔ MLP ➔ L2 Normalization ➔ u(x) ∈ ℝ⁶⁴
│
├── CandidateModel (Movie / Item Tower with Text Transformer & DCN-v2)
│   ├── MovieModel (Feature Extractor)
│   │   ├── Categorical Embeddings               # movie_id, genres, release_year, director_id
│   │   │
│   │   ├── DCN-v2 Stack (Cross Network)         # Explicit Item Attribute Crosses (e.g., genre × director)
│   │   │   ├── Cross Layer 1 (y₀ · yᵀ · W₁ + b₁ + y₁)
│   │   │   └── Cross Layer 2 (y₁ · yᵀ · W₂ + b₂ + y₂)
│   │   │
│   │   └── Text Transformer (Text Encoder)     # Processes unstructured titles, tags, and descriptions
│   │       ├── Token Embeddings                 # Title/description token IDs
│   │       ├── Multi-Head Self-Attention        # Captures semantic text representations
│   │       └── Mean Pooling / [CLS] Extraction # Aggregates text sequence to a single vector
│   │
│   ├── Concatenation & Fusion Layer             # Concat: [DCN-v2 Output | Text Transformer Vector | Static Embeddings]
│   │
│   ├── MLP Stack (Deep Layer)
│   │   ├── Dense Layer 1 (e.g., 128 units + GELU)
│   │   └── Dense Layer 2 (e.g., 64 units)
│   │
│   └── call(inputs)                             # Fusion Vector ➔ MLP ➔ L2 Normalization ➔ v(y) ∈ ℝ⁶⁴
│
└── Execution & Optimization Flow (train_step)
    ├── Forward Tower Passes                    # Compute u(x) and v(y) for mini-batch of size B
    ├── Dot-Product Similarity Matrix           # S = u(x) · v(y)ᵀ  [Shape: B x B]
    ├── Temperature Scaling & Log-Q Correction  # Logits = (S / τ) - (β * log_q)
    └── Cross-Entropy Loss                      # In-Batch Softmax Cross-Entropy Loss
