the training looks stable and the model generalizes as well
as it can to head and torso and tail of movies frequency
distributions, but needs enriched data to better learn
the tail distribution.
more user history such as sentiments.
more movie attributes such as actors, directors, release year,
writers, etc.

-------
in the current train, val ndcg_head_20 curves, the validation, between
epochs 8 and 12, one sees some falls in the ndcg ranking followed
immediatly y increases in the ranking metrics while recall_20
emains smooth,   The sharp local gradient spike seen in ndcg_head_20
is temporarily skewing the adamw values for m_t (=exponential moving average
of the gradient), and v_t (= uncentered variance of the gradient).
The adamw algorithm adapts because m_t filters out high-frequency noise,
and re-projected the embedding weights into stable local min.
