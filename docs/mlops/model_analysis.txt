Training the TwoTowerDNN with an inbatch softmax loss
and using the metric composite_ndcg@20 on the validation set for early stopping,
minimum threshold, and model selection by the keras tuner BayesianOptimization.

The model learning looks healthy.
  - The train vs validation curves for loss and metrics can be seen
    in the sub-directory best_fitting_model_stats.  
The best fitting hyperparameters are also in that subdirectory.
The model is saved to src/test/resources/serving_model

Best fitting model used a batch size of 2056:
   composite ndcg@20 = 0.0234
   head ndcg@20 = 0.0238
   torso ndcg@20 = 0.0236
   tail ndcg@20 = 0.0222
   ndcg@20 = 0.0237
   mrr@20 = 0.0196
   recall@20 = 0.0395
   hit rate = 0.026

The ndcg@20 metrics are ~ 5 times random performance.
The mrr@20 is ~ 6 times random performance.
The recall@20 ~ 3 times random performance.

Compare to these expected ranges and values for random performance:
for k=20, can expect these metric results
Metric  Expected Range      Expected Range    random
        in SOTA production
        (batch size=2048)    (full catalog)   to beat
NDCG@20      0.15 – 0.35     0.08 – 0.18      0.005
MRR@20       0.10 – 0.25     0.06 – 0.12      0.0035
Recall@20    0.30 – 0.55     0.25 – 0.40      0.01


Metric  Expected Range      Expected Range 
        in SOTA production
        (batch size=256)    (full catalog)
NDCG@20    0.45 – 0.65       0.05 – 0.12
MRR@20     0.35 – 0.50       0.04 – 0.09
Recall@20  0.60 – 0.85       0.15 – 0.25

Metric  Expected Range      Expected Range 
        in SOTA production
        (batch size=1024)    (full catalog)
NDCG@20   0.15-0.35 
MRR@20    0.05-0.12 
Recall@20  0.15-0.35


Embeddings:
   the user and candidate embeddings made from these trained models
   were plotted use t-SNE and UMAP in the retrieval github project
   and the results are in the images subdirectory here.

   - there are distinct well separated clusters representing latent
     features.
   - the tier 0,1, and 2 are cohabiting the clusters and not isolated
     into their own clusters and so it appears that popularity bias
     has been successfully avoided.


------------------------------------
Some details of viewing the fit results:

- view the train and val metrics overplotted:
   tensorboard --logdir=bin/rs_pipeline/Trainer/model_run/19/
   or src/test/python/movie_lens_tfx/write_train_val_test_splits_by_user.py
- view the best hyperparameters:
   python3 -m json.tool bin/rs_pipeline/Tuner/best_hyperparameters/18/best_hyperparameters.txt
  view the pipeline Evaluation 
   - on CLI:
     python3 -c "import tensorflow_model_analysis as tfma; print(tfma.load_eval_result('path/to/Evaluator/evaluation/20'))"

==========================
